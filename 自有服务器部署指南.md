# 🚀 销售AI系统 - 自有服务器部署指南

**版本：** V2.0  
**日期：** 2026-02-16  
**目标：** 部署到你的服务器，稳定可靠的生产环境  

---

## 📋 服务器要求

### 最低配置
- CPU：2核
- 内存：2GB
- 硬盘：40GB
- 系统：Ubuntu 20.04+ / CentOS 7+
- 网络：公网IP，1Mbps+

### 推荐配置
- CPU：4核
- 内存：4GB
- 硬盘：80GB
- 系统：Ubuntu 22.04
- 网络：公网IP，5Mbps+

---

## 🎯 快速部署（30分钟）

### 方案A：自动化部署脚本（推荐）

我为你准备了一键部署脚本！

---

## 📦 准备工作

### 1. 获取服务器信息

**记录以下信息：**
- 服务器IP：`____________`
- SSH端口：`____________` (默认22)
- SSH用户：`____________` (通常是root)
- SSH密码/密钥

---

### 2. 本地测试SSH连接

```bash
# 测试连接
ssh root@your_server_ip

# 如果用密钥
ssh -i ~/.ssh/your_key root@your_server_ip
```

---

## 🚀 一键部署脚本

### 脚本1：自动部署脚本

```bash
#!/bin/bash
# 销售AI系统 - 一键部署脚本
# 使用方法：ssh到服务器后执行此脚本

set -e  # 遇到错误立即退出

echo "=================================="
echo "销售AI情报系统 - 自动部署"
echo "=================================="
echo ""

# 检查系统
if [ -f /etc/os-release ]; then
    . /etc/os-release
    OS=$NAME
    echo "检测到系统: $OS"
else
    echo "无法识别系统"
    exit 1
fi

# 更新系统
echo ""
echo "[1/8] 更新系统..."
apt update && apt upgrade -y

# 安装Python
echo ""
echo "[2/8] 安装Python 3.10..."
apt install -y python3.10 python3.10-venv python3-pip git

# 创建部署目录
echo ""
echo "[3/8] 创建部署目录..."
mkdir -p /opt/sales-ai
cd /opt/sales-ai

# 克隆/上传代码（这里先创建占位）
echo ""
echo "[4/8] 准备项目文件..."
echo "请在本地上传项目文件到: /opt/sales-ai/"
read -p "按Enter继续（确保已上传文件）..."

# 创建虚拟环境
echo ""
echo "[5/8] 创建Python虚拟环境..."
python3.10 -m venv venv
source venv/bin/activate

# 安装依赖
echo ""
echo "[6/8] 安装Python依赖..."
pip install --upgrade pip
pip install -r requirements.txt

# 配置防火墙
echo ""
echo "[7/8] 配置防火墙..."
ufw allow 8501/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw --force enable

# 创建systemd服务
echo ""
echo "[8/8] 创建系统服务..."
cat > /etc/systemd/system/sales-ai.service << 'EOF'
[Unit]
Description=Sales AI Intelligence System
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/sales-ai
Environment="PATH=/opt/sales-ai/venv/bin"
ExecStart=/opt/sales-ai/venv/bin/streamlit run app.py --server.address=0.0.0.0 --server.port=8501 --server.headless=true
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable sales-ai
systemctl start sales-ai

echo ""
echo "=================================="
echo "部署完成！"
echo "=================================="
echo ""
echo "访问地址："
echo "  http://$(curl -s ifconfig.me):8501"
echo ""
echo "管理命令："
echo "  启动: systemctl start sales-ai"
echo "  停止: systemctl stop sales-ai"
echo "  重启: systemctl restart sales-ai"
echo "  状态: systemctl status sales-ai"
echo "  日志: journalctl -u sales-ai -f"
echo ""
```

---

## 📝 手动部署步骤（详细版）

### 步骤1：连接服务器

```bash
# SSH连接
ssh root@your_server_ip
```

---

### 步骤2：更新系统

```bash
# Ubuntu/Debian
apt update && apt upgrade -y

# CentOS/RHEL
yum update -y
```

---

### 步骤3：安装Python和依赖

```bash
# Ubuntu/Debian
apt install -y python3.10 python3.10-venv python3-pip git wget curl

# CentOS/RHEL
yum install -y python3 python3-pip git wget curl
```

---

### 步骤4：上传项目文件

**方法1：scp上传（推荐）**

在**本地Mac**执行：
```bash
# 打包项目
cd ~/Desktop
tar -czf 销售AI情报系统.tar.gz 销售AI情报系统/

# 上传到服务器
scp 销售AI情报系统.tar.gz root@your_server_ip:/opt/

# SSH到服务器解压
ssh root@your_server_ip
cd /opt
tar -xzf 销售AI情报系统.tar.gz
mv 销售AI情报系统 sales-ai
```

---

**方法2：Git部署（如果有仓库）**

```bash
cd /opt
git clone https://your-repo-url.git sales-ai
cd sales-ai
```

---

**方法3：直接创建项目（手动复制文件）**

```bash
mkdir -p /opt/sales-ai
cd /opt/sales-ai

# 然后用scp单独上传每个文件
# scp ~/Desktop/销售AI情报系统/app.py root@ip:/opt/sales-ai/
```

---

### 步骤5：创建虚拟环境

```bash
cd /opt/sales-ai

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级pip
pip install --upgrade pip
```

---

### 步骤6：安装依赖

```bash
# 确保在虚拟环境中
source venv/bin/activate

# 安装项目依赖
pip install -r requirements.txt

# 如果没有requirements.txt，手动安装
pip install streamlit openai chromadb sentence-transformers pandas numpy plotly
```

---

### 步骤7：配置环境变量（可选）

```bash
# 创建环境配置
nano /opt/sales-ai/.env
```

**内容：**
```bash
# OpenAI API配置
OPENAI_API_KEY=sk-proj-...

# 数据库配置
DATABASE_PATH=/opt/sales-ai/data/sales.db

# ChromaDB配置
CHROMADB_PATH=/opt/sales-ai/data/chromadb
```

---

### 步骤8：测试运行

```bash
cd /opt/sales-ai
source venv/bin/activate

# 测试运行
streamlit run app.py --server.address=0.0.0.0 --server.port=8501

# 如果正常，Ctrl+C停止
```

**测试访问：**
```
http://your_server_ip:8501
```

---

### 步骤9：配置systemd服务（开机自启）

```bash
# 创建服务文件
nano /etc/systemd/system/sales-ai.service
```

**内容：**
```ini
[Unit]
Description=Sales AI Intelligence System
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/sales-ai
Environment="PATH=/opt/sales-ai/venv/bin"
ExecStart=/opt/sales-ai/venv/bin/streamlit run app.py --server.address=0.0.0.0 --server.port=8501 --server.headless=true
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

---

**启用服务：**
```bash
# 重载配置
systemctl daemon-reload

# 启用开机自启
systemctl enable sales-ai

# 启动服务
systemctl start sales-ai

# 查看状态
systemctl status sales-ai

# 查看日志
journalctl -u sales-ai -f
```

---

### 步骤10：配置防火墙

```bash
# Ubuntu (ufw)
ufw allow 8501/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable

# CentOS (firewalld)
firewall-cmd --permanent --add-port=8501/tcp
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --reload
```

---

## 🌐 配置域名和HTTPS（推荐）

### 步骤11：安装Nginx反向代理

```bash
# 安装Nginx
apt install -y nginx

# 或CentOS
yum install -y nginx
```

---

### 步骤12：配置Nginx

```bash
# 创建配置文件
nano /etc/nginx/sites-available/sales-ai
```

**内容：**
```nginx
server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名

    location / {
        proxy_pass http://localhost:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket支持
        proxy_read_timeout 86400;
    }
}
```

**启用配置：**
```bash
# Ubuntu
ln -s /etc/nginx/sites-available/sales-ai /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx

# CentOS
cp /etc/nginx/sites-available/sales-ai /etc/nginx/conf.d/sales-ai.conf
nginx -t
systemctl restart nginx
```

---

### 步骤13：配置HTTPS（Let's Encrypt）

```bash
# 安装certbot
apt install -y certbot python3-certbot-nginx

# 获取SSL证书
certbot --nginx -d your-domain.com

# 按提示输入邮箱，同意协议

# 测试自动续期
certbot renew --dry-run
```

---

## 🔒 安全加固

### 1. 修改SSH端口

```bash
nano /etc/ssh/sshd_config

# 修改
Port 22  →  Port 2222

# 重启SSH
systemctl restart sshd

# 防火墙允许新端口
ufw allow 2222/tcp
```

---

### 2. 禁用root SSH登录

```bash
# 先创建普通用户
adduser deploy
usermod -aG sudo deploy

# 修改SSH配置
nano /etc/ssh/sshd_config

# 设置
PermitRootLogin no

# 重启SSH
systemctl restart sshd
```

---

### 3. 配置fail2ban

```bash
# 安装
apt install -y fail2ban

# 配置
cat > /etc/fail2ban/jail.local << 'EOF'
[DEFAULT]
bantime = 3600
findtime = 600
maxretry = 5

[sshd]
enabled = true
EOF

# 启动
systemctl enable fail2ban
systemctl start fail2ban
```

---

### 4. 添加应用层访问控制

在 `app.py` 开头添加密码验证：

```python
import streamlit as st

def check_password():
    """密码验证"""
    if "authenticated" not in st.session_state:
        st.session_state.authenticated = False
    
    if not st.session_state.authenticated:
        st.title("🔒 销售AI情报系统")
        st.markdown("---")
        
        username = st.text_input("用户名")
        password = st.text_input("密码", type="password")
        
        if st.button("登录", use_container_width=True):
            # 简单验证（生产环境建议用数据库）
            if username == "admin" and password == "your_secure_password":
                st.session_state.authenticated = True
                st.session_state.username = username
                st.rerun()
            else:
                st.error("❌ 用户名或密码错误")
        
        st.stop()

# 在主程序开头调用
check_password()
```

---

## 📊 监控和日志

### 查看服务状态

```bash
# 查看服务状态
systemctl status sales-ai

# 查看实时日志
journalctl -u sales-ai -f

# 查看最近100行日志
journalctl -u sales-ai -n 100

# 查看错误日志
journalctl -u sales-ai -p err
```

---

### 系统资源监控

```bash
# 实时监控
htop

# 或
top

# 磁盘使用
df -h

# 内存使用
free -h

# 网络连接
netstat -tulpn | grep 8501
```

---

### 设置日志轮转

```bash
# 创建日志轮转配置
nano /etc/logrotate.d/sales-ai
```

**内容：**
```
/opt/sales-ai/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    notifempty
    create 0640 root root
    sharedscripts
    postrotate
        systemctl reload sales-ai > /dev/null
    endscript
}
```

---

## 🔄 更新和维护

### 更新应用

```bash
# 停止服务
systemctl stop sales-ai

# 备份当前版本
cd /opt
tar -czf sales-ai-backup-$(date +%Y%m%d).tar.gz sales-ai/

# 上传新版本（覆盖文件）
# scp ...

# 或git拉取
cd /opt/sales-ai
git pull

# 更新依赖
source venv/bin/activate
pip install -r requirements.txt --upgrade

# 启动服务
systemctl start sales-ai

# 查看日志确认
journalctl -u sales-ai -f
```

---

### 数据库备份

```bash
# 创建备份脚本
nano /opt/backup-sales-ai.sh
```

**内容：**
```bash
#!/bin/bash
BACKUP_DIR="/opt/backups"
DATE=$(date +%Y%m%d-%H%M%S)

mkdir -p $BACKUP_DIR

# 备份数据库
cp /opt/sales-ai/data/sales.db $BACKUP_DIR/sales-$DATE.db

# 备份ChromaDB
tar -czf $BACKUP_DIR/chromadb-$DATE.tar.gz /opt/sales-ai/data/chromadb/

# 删除30天前的备份
find $BACKUP_DIR -type f -mtime +30 -delete

echo "备份完成: $DATE"
```

**设置定时备份：**
```bash
# 添加执行权限
chmod +x /opt/backup-sales-ai.sh

# 添加到crontab（每天凌晨2点）
crontab -e

# 添加这行
0 2 * * * /opt/backup-sales-ai.sh >> /var/log/sales-ai-backup.log 2>&1
```

---

## 🎯 性能优化

### 1. 调整Streamlit配置

创建 `.streamlit/config.toml`：

```toml
[server]
address = "0.0.0.0"
port = 8501
headless = true
runOnSave = false
maxUploadSize = 200

[browser]
gatherUsageStats = false
serverAddress = "your-domain.com"
serverPort = 443

[runner]
magicEnabled = false
fastReruns = true

[theme]
primaryColor = "#1E40AF"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F9FAFB"
textColor = "#1F2937"
```

---

### 2. 使用Gunicorn（可选，更稳定）

虽然Streamlit内置服务器够用，但如果需要更高性能：

```bash
pip install gunicorn

# 创建WSGI包装
nano /opt/sales-ai/wsgi.py
```

**但Streamlit不推荐用Gunicorn，继续用Streamlit自带服务器即可。**

---

### 3. 启用缓存

在代码中使用Streamlit缓存：

```python
@st.cache_data
def load_data():
    # 加载数据
    pass

@st.cache_resource
def init_model():
    # 初始化模型
    pass
```

---

## 📱 移动端优化

### 添加PWA支持（可选）

创建 `static/manifest.json`：

```json
{
  "name": "销售AI情报系统",
  "short_name": "销售AI",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#1E40AF",
  "theme_color": "#1E40AF",
  "icons": [
    {
      "src": "/static/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/static/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

---

## 🚨 故障排查

### 常见问题

**问题1：服务启动失败**
```bash
# 查看详细错误
journalctl -u sales-ai -n 50 --no-pager

# 检查端口占用
netstat -tulpn | grep 8501

# 检查Python环境
/opt/sales-ai/venv/bin/python --version
```

---

**问题2：无法访问**
```bash
# 检查防火墙
ufw status

# 检查服务状态
systemctl status sales-ai

# 检查Nginx
nginx -t
systemctl status nginx
```

---

**问题3：性能慢**
```bash
# 查看系统资源
htop

# 查看进程
ps aux | grep streamlit

# 查看内存
free -h
```

---

## 📋 部署检查清单

- [ ] 服务器准备完成（系统更新、Python安装）
- [ ] 项目文件上传完成
- [ ] 虚拟环境创建完成
- [ ] 依赖安装完成
- [ ] 服务配置完成（systemd）
- [ ] 防火墙配置完成
- [ ] 可以通过IP访问
- [ ] Nginx反向代理配置（可选）
- [ ] 域名解析配置（可选）
- [ ] HTTPS证书配置（可选）
- [ ] 访问密码设置
- [ ] 备份脚本配置
- [ ] 监控日志配置
- [ ] 性能测试完成
- [ ] 移动端测试完成

---

## 🎯 快速命令参考

```bash
# 服务管理
systemctl start sales-ai      # 启动
systemctl stop sales-ai       # 停止
systemctl restart sales-ai    # 重启
systemctl status sales-ai     # 状态

# 日志查看
journalctl -u sales-ai -f     # 实时日志
journalctl -u sales-ai -n 100 # 最近100行

# 更新代码
cd /opt/sales-ai
git pull
systemctl restart sales-ai

# 备份数据
/opt/backup-sales-ai.sh

# 查看访问
tail -f /var/log/nginx/access.log
```

---

## 🌐 访问地址

**开发测试：**
```
http://your_server_ip:8501
```

**生产环境（配置域名后）：**
```
https://your-domain.com
```

---

## 📞 需要帮助？

**如果遇到问题：**
1. 查看日志：`journalctl -u sales-ai -f`
2. 检查状态：`systemctl status sales-ai`
3. 测试连接：`curl http://localhost:8501`
4. 查看端口：`netstat -tulpn | grep 8501`

---

**祝部署顺利！** 🚀

---

**版本：** V2.0  
**制作：** OpenClaw AI  
**日期：** 2026-02-16
