# 🌐 销售AI系统 - 远程访问配置指南

**版本：** V1.0  
**日期：** 2026-02-16  
**目标：** 让同事从任何地方访问系统  

---

## 🎯 三种远程访问方案

| 方案 | 速度 | 难度 | 成本 | 推荐度 |
|------|------|------|------|--------|
| **ngrok** | ⭐⭐⭐⭐⭐ | ⭐ | 免费 | ⭐⭐⭐⭐ |
| **Tailscale** | ⭐⭐⭐⭐ | ⭐⭐ | 免费 | ⭐⭐⭐⭐⭐ |
| **VPS部署** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 付费 | ⭐⭐⭐⭐⭐ |

---

## 🚀 方案1：ngrok（最快速 - 5分钟）

### 适合场景
- ✅ 临时演示/测试
- ✅ 快速让同事访问
- ✅ 不需要配置服务器
- ⚠️ 免费版URL会变化
- ⚠️ 不适合长期使用

---

### 安装 ngrok

#### Mac安装：

**方法1：Homebrew（推荐）**
```bash
brew install ngrok
```

**方法2：直接下载**
```bash
# 访问 https://ngrok.com/download
# 下载Mac版本
# 解压到 /usr/local/bin/
```

---

### 注册ngrok账号

1. 访问：https://dashboard.ngrok.com/signup
2. 注册免费账号（或用Google登录）
3. 获取你的Authtoken

---

### 配置 ngrok

```bash
# 添加你的authtoken（从dashboard获取）
ngrok config add-authtoken YOUR_AUTH_TOKEN
```

---

### 启动内网穿透

```bash
# 确保Streamlit正在运行（在另一个终端）
cd ~/Desktop/销售AI情报系统
streamlit run app.py

# 在新终端启动ngrok
ngrok http 8501
```

---

### 获取访问地址

**ngrok启动后会显示：**
```
Session Status                online
Account                       YourName (Plan: Free)
Version                       3.x.x
Region                        United States (us)
Latency                       50ms
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abc123.ngrok-free.app -> http://localhost:8501

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

**公网地址：** `https://abc123.ngrok-free.app`

---

### 分享给同事

**发送给同事：**
```
🌐 销售AI系统远程访问地址：
https://abc123.ngrok-free.app

📝 说明：
• 可以从任何地方访问
• 无需VPN或同一WiFi
• 手机/电脑都可以用
• 24小时内有效

⚠️ 注意：
• 首次访问会有ngrok提示页
• 点击"Visit Site"继续
```

---

### ngrok优缺点

**优点：**
- ✅ 5分钟完成配置
- ✅ 免费使用
- ✅ HTTPS安全连接
- ✅ 支持任何设备访问
- ✅ 无需配置服务器

**缺点：**
- ⚠️ 免费版URL每次重启会变
- ⚠️ 有ngrok中间提示页
- ⚠️ 不适合长期生产使用
- ⚠️ 速度可能受影响

---

### ngrok付费版（可选）

**价格：** $8/月

**额外功能：**
- 固定域名（不会变）
- 移除中间提示页
- 更多并发连接
- 更快的速度

---

## 🔒 方案2：Tailscale（最安全 - 15分钟）

### 适合场景
- ✅ 团队内部使用
- ✅ 长期稳定访问
- ✅ 安全加密连接
- ✅ 免费最多100设备
- ✅ 像局域网一样访问

---

### 什么是Tailscale？

**简单理解：**
- 创建一个虚拟局域网
- 你和同事都加入这个网络
- 就像在同一WiFi下
- 全程加密，非常安全

---

### 安装Tailscale

#### Mac安装（主机）：

**方法1：Homebrew**
```bash
brew install tailscale
```

**方法2：官网下载**
```
https://tailscale.com/download/mac
```

#### 同事安装（客户端）：

**iOS：** App Store搜索"Tailscale"  
**Android：** Google Play搜索"Tailscale"  
**Windows：** https://tailscale.com/download/windows  
**Mac：** https://tailscale.com/download/mac  

---

### 配置Tailscale

#### 1. Mac上启动Tailscale

```bash
# 启动Tailscale
sudo tailscaled install-system-daemon
tailscale up
```

**会弹出浏览器：**
- 登录Tailscale账号（用Google登录）
- 授权这台Mac

---

#### 2. 获取Tailscale IP

```bash
# 查看你的Tailscale IP
tailscale ip -4
```

**输出示例：**
```
100.64.123.45
```

这就是你的虚拟局域网IP！

---

#### 3. 同事加入网络

**让同事：**
1. 安装Tailscale（对应平台）
2. 登录同一个Tailscale账号
3. 或者：你给他们发送邀请链接

**邀请链接获取：**
```
https://login.tailscale.com/admin/settings/users
→ 点击"Invite user"
→ 发送邀请链接
```

---

#### 4. 同事访问系统

**同事设备上：**
1. 打开Tailscale APP
2. 连接到网络
3. 浏览器访问：

```
http://100.64.123.45:8501
```

（替换为你的Tailscale IP）

---

### Tailscale优缺点

**优点：**
- ✅ 完全免费（最多100设备）
- ✅ 端到端加密，非常安全
- ✅ IP地址固定不变
- ✅ 速度快（点对点连接）
- ✅ 支持所有平台
- ✅ 适合长期使用

**缺点：**
- ⚠️ 需要每个人安装Tailscale
- ⚠️ 需要登录Tailscale账号
- ⚠️ 比ngrok复杂一点

---

## 🚀 方案3：VPS部署（最专业 - 2小时）

### 适合场景
- ✅ 正式生产环境
- ✅ 多人长期使用
- ✅ 需要稳定可靠
- ✅ 需要独立域名
- 💰 需要付费服务器

---

### 云服务器选择

| 服务商 | 价格/月 | 配置 | 推荐 |
|--------|---------|------|------|
| **阿里云** | ¥60+ | 2核2G | ⭐⭐⭐⭐ |
| **腾讯云** | ¥60+ | 2核2G | ⭐⭐⭐⭐ |
| **华为云** | ¥60+ | 2核2G | ⭐⭐⭐⭐ |
| **DigitalOcean** | $6/月 | 1核1G | ⭐⭐⭐⭐⭐ |
| **Vultr** | $6/月 | 1核1G | ⭐⭐⭐⭐ |

---

### 部署步骤（以阿里云为例）

#### 1. 购买云服务器

**配置推荐：**
- CPU：2核
- 内存：2GB
- 硬盘：40GB
- 系统：Ubuntu 22.04
- 带宽：1Mbps（够用）

---

#### 2. 配置服务器

**SSH连接：**
```bash
ssh root@your_server_ip
```

**更新系统：**
```bash
apt update && apt upgrade -y
```

**安装Python：**
```bash
apt install python3 python3-pip -y
```

---

#### 3. 上传项目

**方法1：git（推荐）**
```bash
cd /opt
git clone https://your-repo.git
cd 销售AI情报系统
```

**方法2：scp上传**
```bash
# 在本地Mac执行
scp -r ~/Desktop/销售AI情报系统 root@your_server_ip:/opt/
```

---

#### 4. 安装依赖

```bash
cd /opt/销售AI情报系统
pip3 install -r requirements.txt
```

---

#### 5. 配置防火墙

```bash
# 允许8501端口
ufw allow 8501
ufw enable
```

---

#### 6. 使用systemd管理（开机自启）

**创建服务文件：**
```bash
nano /etc/systemd/system/sales-ai.service
```

**内容：**
```ini
[Unit]
Description=Sales AI System
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/销售AI情报系统
ExecStart=/usr/local/bin/streamlit run app.py --server.address=0.0.0.0 --server.port=8501
Restart=always

[Install]
WantedBy=multi-user.target
```

**启动服务：**
```bash
systemctl enable sales-ai
systemctl start sales-ai
systemctl status sales-ai
```

---

#### 7. 配置Nginx反向代理（可选）

**安装Nginx：**
```bash
apt install nginx -y
```

**配置文件：**
```bash
nano /etc/nginx/sites-available/sales-ai
```

**内容：**
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

**启用配置：**
```bash
ln -s /etc/nginx/sites-available/sales-ai /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

---

#### 8. 配置HTTPS（Let's Encrypt）

```bash
# 安装certbot
apt install certbot python3-certbot-nginx -y

# 获取SSL证书
certbot --nginx -d your-domain.com
```

---

#### 9. 访问系统

**HTTP：**
```
http://your-server-ip:8501
```

**HTTPS（配置域名后）：**
```
https://your-domain.com
```

---

### VPS部署优缺点

**优点：**
- ✅ 最稳定可靠
- ✅ 完全掌控
- ✅ 可以配置域名
- ✅ 支持HTTPS
- ✅ 适合正式环境
- ✅ 不限制并发用户

**缺点：**
- 💰 需要付费（约¥60/月）
- ⚠️ 需要服务器运维知识
- ⚠️ 配置相对复杂
- ⚠️ 需要维护更新

---

## 📊 方案对比总结

### 快速对比表

| 对比项 | ngrok | Tailscale | VPS |
|--------|-------|-----------|-----|
| **速度** | 5分钟 | 15分钟 | 2小时 |
| **成本** | 免费 | 免费 | ¥60/月 |
| **稳定性** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **安全性** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **易用性** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| **长期使用** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

### 场景推荐

**临时演示（1-2天）：**
→ **ngrok**
- 最快速度
- 无需配置
- 立即可用

**团队内测（1-3个月）：**
→ **Tailscale**
- 安全稳定
- 完全免费
- 固定IP

**正式上线（长期）：**
→ **VPS部署**
- 最稳定
- 独立域名
- 专业形象

---

## 🎯 推荐方案

### 阶段1：现在（立即测试）
**使用 ngrok**
```bash
# 5分钟启动
brew install ngrok
ngrok http 8501
# 分享链接给同事
```

---

### 阶段2：团队内测（1-2周后）
**升级到 Tailscale**
```bash
# 15分钟配置
brew install tailscale
tailscale up
# 邀请同事加入
```

---

### 阶段3：正式上线（1个月后）
**部署到 VPS**
```bash
# 购买阿里云/腾讯云
# 部署到云服务器
# 配置独立域名
```

---

## 📝 快速操作手册

### ngrok 5分钟快速部署

```bash
# 1. 安装ngrok
brew install ngrok

# 2. 注册并配置（只需一次）
# 访问 https://dashboard.ngrok.com/get-started/setup
ngrok config add-authtoken YOUR_TOKEN

# 3. 启动系统（终端1）
cd ~/Desktop/销售AI情报系统
streamlit run app.py

# 4. 启动ngrok（终端2）
ngrok http 8501

# 5. 复制公网地址
# 输出中的 https://xxxx.ngrok-free.app

# 6. 分享给同事
```

---

### Tailscale 15分钟快速部署

```bash
# 1. 安装Tailscale
brew install tailscale

# 2. 启动并登录
tailscale up
# 浏览器会弹出，用Google登录

# 3. 获取IP
tailscale ip -4
# 记下IP，例如：100.64.123.45

# 4. 确保系统运行
cd ~/Desktop/销售AI情报系统
streamlit run app.py --server.address=0.0.0.0

# 5. 邀请同事
# 访问 https://login.tailscale.com/admin/settings/users
# 发送邀请链接

# 6. 同事访问
# 同事安装Tailscale后访问：
# http://100.64.123.45:8501
```

---

## 🔒 安全建议

### 所有方案都应该：

**1. 添加访问密码**

创建 `.streamlit/secrets.toml`：
```toml
password = "your_secure_password"
```

在 `app.py` 开头添加：
```python
import streamlit as st

def check_password():
    if "authenticated" not in st.session_state:
        st.session_state.authenticated = False
    
    if not st.session_state.authenticated:
        password = st.text_input("请输入访问密码", type="password")
        if st.button("登录"):
            if password == st.secrets["password"]:
                st.session_state.authenticated = True
                st.rerun()
            else:
                st.error("密码错误")
        st.stop()

check_password()
```

---

**2. 限制访问IP（VPS方案）**

```bash
# 只允许特定IP访问
ufw allow from 公司IP to any port 8501
```

---

**3. 定期更新**

```bash
# 定期更新系统和依赖
apt update && apt upgrade -y
pip install --upgrade -r requirements.txt
```

---

## 📞 需要帮助？

### 常见问题

**Q1: ngrok链接太长，能缩短吗？**
A: 付费版可以自定义域名，或用短网址服务。

**Q2: Tailscale速度慢怎么办？**
A: 确保两端都有公网IP，或配置DERP中继服务器。

**Q3: VPS太贵，有便宜方案吗？**
A: 可以试试Oracle Cloud永久免费套餐。

**Q4: 能同时使用多个方案吗？**
A: 可以！不冲突。

---

## 🎉 总结

**立即可用（现在）：**
```bash
brew install ngrok
ngrok http 8501
```

**最佳方案（推荐）：**
```bash
brew install tailscale
tailscale up
```

**生产环境（未来）：**
```
购买VPS + 部署系统 + 配置域名
```

---

**选择建议：**
- 🚀 **现在就要** → ngrok
- 💯 **团队使用** → Tailscale
- 🏢 **正式上线** → VPS

---

**祝远程访问顺利！** 🌐

---

**版本：** V1.0  
**制作：** OpenClaw AI  
**日期：** 2026-02-16
